import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class UserinterfaceTest {

//    @Test
//    void startProgramTest() {
//        Userinterface ui = new Userinterface();
//        String[] argument = new String[1];
//        argument[0] = "Invalid argument";
//        assertEquals("Structure should be: java TopSecret [argument]",ui.startProgram(argument), "Invalid argument input should be invalid");
//    }

    /**test to make
     * no inputs, correct
     * one input, correct
     * one input, correct (different numbers)
     * one input, incorrect (contains letters and numbers)
     * one input, incorrect (contains nothing)
     * one input, incorrect (contains just letters)
     * multiple inputs, incorrect (two number arguments)
     * multiple inputs, incorrect (two letter arguments)
     * multiple inputs, incorrect (one number and one letter argument)
     *
     */
}
